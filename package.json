{
  "name": "neptune-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node undefined rupak-nm/neptune-subgraph",
    "create-local": "graph create --node http://localhost:8020/ rupak-nm/neptune-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ rupak-nm/neptune-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rupak-nm/neptune-subgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.41.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
